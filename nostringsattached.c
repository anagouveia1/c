#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(int argc, string argv[])
{
    // iterate over strings in argv
    for (int i = 0; i < argc; i++)
    {
        // iterate over characters in current string
        for (int j = 0, size_t = strlen(argv[i]); j < n; j++)
        {
            // print j'th caracter in i'th string
            printf("%c\n", argv[0]);
        }
        
    }

}

// Need to receive input, such as a name:
int main(int argc, string argv[])
//iterate over string and count characters
 for (int i = 0; i < argc; i++)
 
//need to move things around so argv[0] stores i count, but before it does, you need to extend the size of the string and move characters...
//boy this sounds like an awfully complicated process.. onto the next one.
