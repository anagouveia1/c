#include <ctype.h>
#include <stdbool.h>
#include <stdio.h>
#include <string.h>

bool upword(const char *word)
{
//Use a bubble sort mechanism to go through the string characters if buble sort doesn't move anything out of order = Upward

if (char != 2)
    {
        printf("missing command-line argument\n");
        return 1;
    }
    printf("hello, %s\n", char[1]);
    return 0;
}